# Deploy configuration with grouped services

# Grouped services (executed in parallel within each group)
groups:
  # Migrate services group
  migrate:
    - name: proezd-claim-migrate
      directory: proezd-claim-migrate
      gitlab_project: ecp/proezd/proezd-claim-migrate

    - name: proezd-contract-migrate
      directory: proezd-contract-migrate
      gitlab_project: ecp/proezd/proezd-contract-migrate
